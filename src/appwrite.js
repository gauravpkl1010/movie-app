{
  /*import { Client, Databases } from "appwrite";
const DATABASE = import.meta.env.DATABASE_ID;
const COLLECTION = import.meta.env.COLLECTION_ID;
const PROJECT = import.meta.env.PROJECT_ID;
const client=new Client().setEndpoint('https://cloud.appwrite.io/v1').setProject(PROJECT);
const database=new Databases(client);
export const updateSearchCount = async (search,movie) => {
  console.log("Updating search count");
};
*/
}
